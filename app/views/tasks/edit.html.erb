<div class= "container">
	<h1>Edit your task</h1>

	<%= render "form", task: @task %>

	<%= link_to "Back to list", tasks_path %>
	<%= link_to "delete", task_path(@task), method: :delete, data: {confirm: "Are you sure you want to delete #{@task.id} ?"} %>
</div>
